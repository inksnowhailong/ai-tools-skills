---
name: coding-rules
description: 编码风格、注释规范、Tailwind/UnoCSS 样式与 data-alt 规范
default: on
---

# 编写规则

## 核心原则

- 严格遵守用户指定的任务需求，避免添加未经请求的功能
- 不要过度设计：不添加边界条件、验证逻辑、错误处理（除非明确需要）
- 默认使用最简单直接的解决方案
- 不创建不必要的文件、类、函数或配置

## 实现原则

- 不会过度设计。
- 在实现功能时，先准确划分功能的复杂程度，优先选择最简单、最有效的方案。
- 只有在必要时，才进行适当的抽象与封装。
- 喜欢在代码中加入清晰、简洁的中文注释，帮助其他开发者快速理解设计思路和实现逻辑。

## 功能解决方案设计

**编写代码前**，以纯文本形式概述 1-3 个最佳实践解决方案：

- 每个方案用 3-5 句话描述：核心概念、优点、缺点、适用场景
- **等待用户确认或选择方案后**才生成代码

## 注释规范

- 为所有状态变量和函数添加文档注释，描述业务功能（按语言选对应格式：JS/TS 用 JSDoc、Python 用 docstring、Java/C# 用 Javadoc/XML doc 等）
- 简单代码（getter/setter、单行代码）省略注释
- 复杂函数使用结构化文档注释（参数、返回值、异常）
- 业务逻辑代码中不编写示例实现或单元测试（除非明确要求）
- 不使用调试输出语句（除非必要）

## 样式规则

### CSS 框架
- 所有样式使用 **Tailwind CSS** 或 **UnoCSS**（根据项目配置）
- 禁止使用原生 CSS（内联样式、.css 文件）
- 禁止使用其他框架（Less、Sass）

### HTML 标签规范
为所有有意义的标签添加 `data-alt` 属性：

```html
<div data-alt="main-content" class="p-4 bg-gray-100">...</div>
<button data-alt="submit-button" class="btn-primary">提交</button>
```

- `data-alt` 值应简短、准确、具有描述性
- 非语义标签（空包装器）可忽略
- 代码分析时指出缺失的 `data-alt` 属性
